# MySQL connection URL (docker-compose uses service name `mysql` as host)
DB_URL=mysql+pymysql://appuser:StrongPass@mysql:3306/onepiecedb
# Where to store uploads inside the container
UPLOAD_DIR=/app/uploads
# FastAPI settings
APP_TITLE=One Piece Registration (FastAPI)
APP_DEBUG=false
